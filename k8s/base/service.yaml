apiVersion: v1
kind: Service
metadata:
  name: tabular-mlops-api
  labels:
    app.kubernetes.io/name: tabular-mlops-api
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: tabular-mlops-api
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
